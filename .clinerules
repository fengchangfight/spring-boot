# Cline 项目规则 - springboot 源码阅读

## 核心规则：源码阅读知识沉淀

当用户与你进行 springboot 源码阅读探讨时，你必须将每次讨论中涉及的以下内容**持续追加**到 `docs/codereading.md` 文件中：

1. **架构分析**：模块结构、分层设计、依赖关系、设计模式
2. **核心逻辑**：关键类/函数的职责、执行流程、数据流转
3. **关键代码路径**：从入口到出口的完整调用链
4. **设计决策**：为什么这样设计、有哪些权衡
5. **扩展机制**：插件系统、抽象基类、接口约定、注册机制
6. **重要概念**：术语定义、核心抽象（如 Runnable、Tool、Agent 等）

### 写入规则

- 使用中文撰写，保持 Markdown 格式清晰
- 按包/模块组织，使用二级标题（##）区分模块，三级标题（###）区分子主题
- 每次写入前先回读 `docs/codereading.md`，在现有内容基础上**追加**，避免覆盖已有知识
- 对于重复或矛盾的内容，应更新覆盖旧内容而非简单追加重复信息
- 内容要精简有料，避免流水账；重点是"为什么"而不是"是什么"
- 对于关键类/函数，标注其文件路径（相对于项目根目录），方便日后快速定位
---

## 新增规则：源码阅读方法论沉淀

当用户要求你总结"如何阅读代码"的方法论时，你必须将以下内容写入 `docs/codereading-tools.md`：

1. **每一步的目标**：这步要回答什么问题（What / Where / How / Why）
2. **使用的工具/命令**：具体用了什么（`read_files`、`search_codebase`、`run_commands`）
3. **为什么这样选择**：为什么先读这个文件而不是那个、为什么用这个搜索词
4. **发现的模式和技巧**：逆依赖搜索、标本选择策略、三件套原则等
5. **具体案例**：以当前分析的项目为例，附上真实的搜索词和结果

### 写入规则

- 使用中文，Markdown 格式
- 内容面向"想学会读代码的人"而非"想了解项目的人"
- 重点是**思维过程**（为什么这样做），而非操作日志（做了什么）
- 每个技巧附一个本次分析的真实例子
- 在文档末尾维护"工具箱速查"表格和"思维模型"流程图